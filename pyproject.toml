[tool.poetry]
name = "stability-matrix-tools"
version = "0.3.4"
description = ""
authors = ["Ionite <dev@ionite.io>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.10, <3.13"
typer = {extras = ["all"], version = "^0.12.5"}
pydantic = {extras = ["dotenv"], version = "^2.4.2"}
semver = "^3.0.2"
httpx = "^0.27.2"
b2sdk = "^2.5.1"
python-dotenv = "^1.0.0"
cryptography = "^43.0.1"
keyring = "^25.4.0"
pyperclip = "^1.8.2"
blake3 = "^0.4.1"
pygithub = "^2.1.1"
yaspin = "^3.0.1"
click = "8.1.7" # Pinned to avoid compatibility issues with Typer

[tool.poetry.scripts]
sm-tools = "stability_matrix_tools.main:app"

[tool.poetry.group.dev.dependencies]
ruff = "^0.1.6"
black = "^23.11.0"
isort = "^5.12.0"
pytest = "^7.4.3"
pre-commit = "^3.6.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
